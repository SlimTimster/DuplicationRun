extends Node


func spawn_gates(count: int = 1):
	print("Spawning " + str(count) + " Gates")
	
	#TODO: 
	# Needs reference to gate scene
	# Spawn a gate which should have a class
	# Math opteration 
	# Number
	# Placement

func spawn_characters(count : int = 1):
	print("Spawning " + str(count) + " Characters")
	
